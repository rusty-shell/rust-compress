before_install:
  - yes | sudo add-apt-repository ppa:hansjorg/rust
  - sudo apt-get update
install:
  - sudo apt-get install rust-nightly
script:
  - rustc --dylib lib.rs
  - rustc --test lib.rs
  - ls
  - ./compress
  - rustdoc --test -L. lib.rs

